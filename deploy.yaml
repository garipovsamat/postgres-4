- name: Configure PostgreSQL master
  hosts: master
  become: true
  vars_files:
    - vars/main.yml  
  roles:
    - postgre-master

- name: Configure PostgreSQL replica
  hosts: replica
  become: true
  vars_files:
    - vars/main.yml 
  roles:
    - postgre-replica